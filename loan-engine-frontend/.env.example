# Local dev (Vite proxy forwards /api → http://localhost:8080)
VITE_API_URL=/api

# Production (set in Vercel dashboard — include /api suffix)
# VITE_API_URL=https://loan-engine-backend.onrender.com/api
